# ============================================
# BeeCastly Environment Configuration
# ============================================
# ⚠️  CRITICAL: This file should ONLY contain system-level secrets
# 
# 👉 .env = Server engine secrets ONLY
# 👉 Dashboard = Business integrations & user APIs
#
# DO NOT PUT HERE:
#   - WhatsApp API tokens
#   - SMS provider credentials (Twilio, MSG91, etc.)
#   - Email SMTP credentials
#   - Payment gateway keys (Stripe, Razorpay)
#   - Telegram bot tokens
# 
# These belong in the DATABASE and are configured via Dashboard UI!
# ============================================

# ============================================
# Server Configuration
# ============================================
NODE_ENV=development
PORT=3001
API_URL=http://localhost:3001
WEB_URL=http://localhost:3000

# ============================================
# Database Configuration (System-level)
# ============================================
DB_HOST=localhost
DB_PORT=5432
DB_NAME=beecastly
DB_USER=beecastly
DB_PASSWORD=your_secure_db_password
DB_SSL=false

# ============================================
# Redis Configuration (System-level)
# ============================================
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_PASSWORD=

# ============================================
# JWT Configuration (System-level)
# ============================================
JWT_SECRET=your_super_secret_jwt_key_min_32_chars_long
JWT_EXPIRES_IN=7d
JWT_REFRESH_SECRET=your_refresh_token_secret_min_32_chars

# ============================================
# Encryption Key (System-level - for DB encryption)
# ============================================
# This key is used to encrypt/decrypt API credentials stored in database
# Generate with: openssl rand -base64 32
ENCRYPTION_KEY=your_encryption_key_min_32_chars_base64_encoded

# ============================================
# OpenAI Configuration (Optional - can be in DB too)
# ============================================
# If not set, users must configure their own OpenAI key via Dashboard
OPENAI_API_KEY=sk-your_openai_key

# ============================================
# Queue Configuration
# ============================================
QUEUE_CONCURRENCY=5
QUEUE_ATTEMPTS=3
QUEUE_BACKOFF_DELAY=1000

# ============================================
# Email (System-level - for platform emails only)
# ============================================
# These are used for system emails (password reset, notifications)
# Users configure their own email integrations in Dashboard
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
SMTP_FROM=noreply@beecastly.com

# ============================================
# Storage Configuration
# ============================================
# AWS S3 or compatible (MinIO, etc.)
S3_ENDPOINT=
S3_BUCKET=
S3_ACCESS_KEY=
S3_SECRET_KEY=
S3_REGION=us-east-1
S3_PUBLIC_URL=

# ============================================
# Monitoring & Logging
# ============================================
LOG_LEVEL=info
SENTRY_DSN=

# ============================================
# Feature Flags
# ============================================
ENABLE_AI_FEATURES=true
ENABLE_VOICE_CALLS=true
ENABLE_PAYMENTS=true
ENABLE_WHITELABEL=true

# ============================================
# Security Settings
# ============================================
# CORS origins (comma-separated)
CORS_ORIGINS=http://localhost:3000,http://localhost:3001

# Rate limiting
RATE_LIMIT_WINDOW_MS=60000
RATE_LIMIT_MAX_REQUESTS=100

# ============================================
# Development Settings
# ============================================
# Set to true to skip certain validations in dev
DEV_SKIP_WEBHOOK_VERIFICATION=false
